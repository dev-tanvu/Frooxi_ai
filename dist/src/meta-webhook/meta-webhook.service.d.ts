import { ConfigService } from '@nestjs/config';
import { PrismaService } from '../prisma/prisma.service';
import { GeminiService } from '../ai/gemini.service';
import { MetaApiService } from './meta-api.service';
import { RedisService } from '../redis/redis.service';
import { PineconeService } from '../ai/pinecone.service';
import { HttpService } from '@nestjs/axios';
import { ProductService } from '../product/product.service';
import { OrderService } from '../order/order.service';
import { CustomerService } from '../customer/customer.service';
import { FaqService } from '../faq/faq.service';
import { AiAgentService } from '../ai-agent/ai-agent.service';
import { SettingsService } from '../settings/settings.service';
import { EncryptionService } from '../common/encryption.service';
export declare class MetaWebhookService {
    private configService;
    private prisma;
    private gemini;
    private metaApi;
    private redis;
    private pinecone;
    private httpService;
    private productService;
    private orderService;
    private customerService;
    private faqService;
    private aiAgentService;
    private settingsService;
    private encryption;
    private readonly logger;
    private readonly appId;
    private aiAgentTableMissingWarned;
    private adminOverrides;
    private readonly OVERRIDE_DURATION;
    private typingHeartbeats;
    private lastUserMessageTimestamp;
    private readonly CONFIDENCE_THRESHOLD;
    private activeAiTasks;
    private readonly MAX_CONCURRENT_TASKS;
    private readonly MAX_QUEUE_SIZE;
    private waitingQueue;
    pauseAiForCustomer(customerId: string, durationMinutes?: number): void;
    resumeAiForCustomer(customerId: string): void;
    constructor(configService: ConfigService, prisma: PrismaService, gemini: GeminiService, metaApi: MetaApiService, redis: RedisService, pinecone: PineconeService, httpService: HttpService, productService: ProductService, orderService: OrderService, customerService: CustomerService, faqService: FaqService, aiAgentService: AiAgentService, settingsService: SettingsService, encryption: EncryptionService);
    private acquireAiSlot;
    private releaseAiSlot;
    handleWebhookEvent(event: any): Promise<void>;
    private normalizeMessengerEvent;
    private normalizeWhatsAppEvent;
    private sendDirectPlatformResponse;
    private checkSpam;
    private detectIntentFastPath;
    private extractSearchQueries;
    private processEcho;
    private getOrCreateContext;
    private persistMessage;
    private userMessageTimestamps;
    private spamBlocks;
    private processOrderAgent;
    private processTextAgent;
    private buildProductContext;
    private getCachedSearchResults;
    private startTypingHeartbeat;
    private stopTypingHeartbeat;
    private sendOptimizedResponse;
    private handleBatchImages;
    private processVisualAgent;
    private processVoiceAgent;
    private downloadFile;
    private extractAndProcessOrder;
    private extractTagJson;
    private stripMarkdown;
    private detectInfoCategory;
    private downloadImage;
    private isAgentActive;
    private getAgentUnavailableMessage;
    private notifyAdminAboutInactiveAgent;
    private extractAndSaveProfile;
    private extractImageUrls;
    private removeImageUrls;
}
