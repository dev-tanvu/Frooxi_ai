import { ConfigService } from '@nestjs/config';
import { WebhookNormalizerService } from './webhook-normalizer.service';
import { WebhookStateService } from './webhook-state.service';
import { IntentRouterService } from './intent-router.service';
import { AgentLifecycleService } from './agent-lifecycle.service';
import { OrderLogicService } from './order-logic.service';
import { GeminiBehaviourService } from '../ai/gemini-behaviour.service';
import { GeminiResponseService } from '../ai/gemini-response.service';
import { GeminiMultimodalService } from '../ai/gemini-multimodal.service';
import { RedisService } from '../redis/redis.service';
import { ProductService } from '../product/product.service';
import { FaqService } from '../faq/faq.service';
import { AiAgentService } from '../ai-agent/ai-agent.service';
import { PineconeService } from '../ai/pinecone.service';
import { CustomerService } from '../customer/customer.service';
import { CartService } from '../cart/cart.service';
import { MetaApiService } from './meta-api.service';
import { HttpService } from '@nestjs/axios';
export declare class MetaWebhookService {
    private configService;
    private normalizer;
    private state;
    private router;
    private lifecycle;
    private orderLogic;
    private geminiBehaviour;
    private geminiResponse;
    private geminiMultimodal;
    private redis;
    private productService;
    private faqService;
    private aiAgentService;
    private pinecone;
    private customerService;
    private cartService;
    private metaApi;
    private httpService;
    private readonly logger;
    private readonly appId;
    constructor(configService: ConfigService, normalizer: WebhookNormalizerService, state: WebhookStateService, router: IntentRouterService, lifecycle: AgentLifecycleService, orderLogic: OrderLogicService, geminiBehaviour: GeminiBehaviourService, geminiResponse: GeminiResponseService, geminiMultimodal: GeminiMultimodalService, redis: RedisService, productService: ProductService, faqService: FaqService, aiAgentService: AiAgentService, pinecone: PineconeService, customerService: CustomerService, cartService: CartService, metaApi: MetaApiService, httpService: HttpService);
    handleWebhookEvent(event: any): Promise<void>;
    private processOrderAgent;
    private handleOrderAgentLogic;
    private processTextAgent;
    private handleTextAgentLogic;
    private processVisualAgent;
    private processVoiceAgent;
    pauseAiForCustomer(customerId: string, durationMinutes?: number): Promise<void>;
    resumeAiForCustomer(customerId: string): Promise<void>;
    private getToken;
    processOrchestratedBatch(messages: any[]): Promise<void>;
}
